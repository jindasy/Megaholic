import javax.swing.*;

public class PlayerController {
    public PlayerController(Player player, KeyStroke keyJump, KeyStroke keySlide) {
        // ActionListener Key Jump -> player.jump()
        // ActionListener Key Slide -> player.slide()
    }
}
